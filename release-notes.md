# 🐾 Sonnet Bot v1.1.0

更名 Sonnet Bot + 真实小脸图标 + 开机自启 + 状态动作特效。

## ✨ 本次新增（v1.1.0）

- **更名 Sonnet Bot**：应用名、窗口标题、菜单、启动器、试玩页统一改为 Sonnet Bot。
- **真实小脸图标**：exe 图标不再是默认电子图标，而是用纯 Node 光栅化小粉圆脸（blob + expression-0 的真实眼形）生成的多分辨率 ICO，任务栏/桌面一眼认出。
- **开机自启**：右键菜单新增「🚀 开机自启」勾选项，调用 `app.setLoginItemSettings`，开机自动上岗，无需手动配置。
- **状态动作特效**：给状态加视觉特效——
  - 🛰️ **轨道**：双层旋转激光环（霓虹椭圆 + 圆，反向自转）
  - 📡 **雷达**：三圈外传波纹向外扩散并淡出
  - ⏳ **加载**：旋转霓虹弧
  - 🚨 **警报**：红色脉冲环
  - 🔦 **搜索/扫描**：横扫光束
  - ❤️ **开心**：爱心上浮　🎉 **庆祝**：彩色彩带迸射　💤 **睡眠**：Z 字上浮　🎵 **哼唱**：音符飘起　✨ **兴奋**：星星闪烁　⋯ **思考**：点点跳动

## 🛠 v1.0.1 修复（仍有效）

- 打包 exe 启动崩溃：日志改写 `userData`，不再写只读 `app.asar`（EROFS）。
- 透明窗口不可见：单实例锁 + `uncaughtException` 兜底 + `ready-to-show` 后显示。
- 启动 bat 无响应：直接调本地 `electron.cmd`，不走 `npx` 拉包。

## ⬇ 使用

下载下方的 `Sonnet.Bot.1.1.0.exe`，**双击即可养桌宠**，无需安装、无需 Node/npm。

> 首次运行 Windows SmartScreen 可能提示「未签名」，点「更多信息 → 仍要运行」即可（开源软件未购买代码签名证书的正常现象）。
