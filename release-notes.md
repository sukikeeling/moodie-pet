# 🐾 Moodie 桌宠 v1.0.1

修复启动崩溃 / 窗口不可见。

## 🐛 本次修复

- **打包 exe 一启动就报错**：`main.js` 把日志写到 `__dirname`，打包后落在只读的 `app.asar` 里 → `EROFS` 崩溃。改为写到 `userData`。
- **透明窗口不可见**：`main.js` 重写为成熟结构——单实例锁、`uncaughtException` 兜底（崩了弹窗而非隐形）、`ready-to-show` 后再显示。
- **启动 bat 无响应**：原 `npx electron .` 在部分网络下会去拉包卡死。改为直接调用本地 `node_modules\.bin\electron.cmd`。

## ✨ 功能（v1.0.0 起具备）

- 39 状态自动轮播 + 状态内表情池循环 + 眨眼
- 39 组工作流台词（思考→「让我想想…」、码字→「文档 ing」、发送→「发出去了！」）
- 拖动 3D 转向（yaw/pitch），松手回正
- 透明置顶 + 桌面穿透
- 右键菜单：颜色 / 配饰（草帽·眼镜·蝴蝶结·披风）/ 尺寸（每次缩 1/3）/ 暂停 / 退出

## ⬇ 使用

下载下方的 `Moodie.Pet.1.0.1.exe`，**双击即可养桌宠**，无需安装、无需 Node/npm。

> 首次运行 Windows SmartScreen 可能提示「未签名」，点「更多信息 → 仍要运行」即可（开源软件未购买代码签名证书的正常现象）。
